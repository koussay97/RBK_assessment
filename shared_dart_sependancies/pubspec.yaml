name: shared_dart_sependancies
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  dartz: ^0.10.1
  mocktail: ^1.0.0
  equatable: ^2.0.5
  build_runner: ^2.4.6
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
